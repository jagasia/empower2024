1) modified "generateToken" method
2) in every project, use the UserInfo entity for all users (admin/ participant/ host/ user/ customer)
3) kindly verify your entity classes as early possible
-------------------------------------
Cloud native technologies


DevOps
--------
What is DevOps?
	Development Operations
		git		(gihub, gitlab, bitbucket)		source code management
		jira		project management tool
		jenkins		build deployment 			CI/CD
		maven		
		docker
		kubernetes

git
---
	remote repository

	local repository		in c:\ or d:\ 

while creating a local repository, ensure that the current directory is not a sub folder of any other repository


c:\training 

	if this folder is a local repository
		then


c:\training\java
	this java folder cannot be a repository for another remote repository at all


what is a local git repository?
	a folder/directory where .git folder is present 

		this folder is created 
			git init


lab setup
	install git

	git config --global user.name jagasia
	git config --gloabl user.email rjagadeeswaran@Yahoo.com




	git config --list --show-origin


once you have installed git
and configured using your
	name and email address

start working


1) create a remote repository 	(in github.com we create a repository)
2) create a local repository		(a folder where you do git init)
	git init	
	git remote add origin https://github.com/jagasia/gd.git

3) create some files like readme

4) git add .
5) git commit -m "first"
6) git branch -M jag
7) git push -u origin jag


learn the git commands and switches



-----------------------------------
we are going to collaborate

settings
	collaborators

the collaborators will be able to push to our git

----------------------------------------------------------
git init
git commit
git add
git remote
git push
git branch
	-M
	-d
	
		how do you delete a local branch?
		how do you delete a remote branch?


--------------------------------------------------------------
Sonar Qube
--------------
for checking the code quality

lab setup
----------
	download sonar from 
	https://www.sonarsource.com/products/sonarqube/downloads/

unzip the folder

in that folder, 
	bin folder
		windows folder
				startSonar.bat


run this batch file
	to start the sonar server.




download sonar qube from:
	https://www.sonarqube.org/success-download-community-edition/

unzip and extract to softwares folder
	
start sonar server:
	in sonar folder, go to bin folder -> windows folder,
	run the StartSonar.bat	script

go to browser 
	http://localhost:9000

it will prompt you to enter username and password
		admin
		admin


sonar qube required java 11

<properties>
       <java.version>1.8</java.version>
       <maven.compiler.source>1.8</maven.compiler.source>
       <maven.compiler.target>1.8</maven.compiler.target>
</properties>

-------------------------------------------------------------------------------------
Demo for using sonarqube to analyze the code quality of a core java project (maven)

1) Create a java project
2) Convert to maven project
	right click project
		Configure-> convert to maven project
3) now you got pom.xml
	add the following plugins:

<plugin>
  <groupId>org.sonarsource.scanner.maven</groupId>
  <artifactId>sonar-maven-plugin</artifactId>
  <version>3.7.0.1746</version>
</plugin>

<plugin>
	<groupId>org.jacoco</groupId>
	<artifactId>jacoco-maven-plugin</artifactId>
	<version>0.8.1</version>
</plugin>



4) clean install the project (any of the following steps)
	i) right click project
		run as -> maven build (first option)
			in the goals:-
				clean org.jacoco:jacoco-maven-plugin:prepare-agent install

	ii) go to cmd prompt to the location where pom.xml is present
		mvn clean org.jacoco:jacoco-maven-plugin:prepare-agent install


	what happens? the jar/war is created in target folder


5) go to localhost:9000 portal
	this is sonarqube portal

	you have already logged in (admin:admin)
	click "Add Project" in the top right corner
		manually

	type the project name and (display name is automatically typed)

	create token	(type anything)
		continue

	choose "Maven"

		click the copy+ in the right side

i) Run the below command in cmd prompt where pom.xml is present:
mvn sonar:sonar -Dsonar.projectKey=jf011-sonarqube-demo-1 -Dsonar.host.url=http://localhost:9000 -Dsonar.login=9d466306414cebdcf29b112c0b0f9aee854e3126

(or)
ii) Right click the project and run as "Maven Build" (2)
	paste the below code in goals

sonar:sonar -Dsonar.projectKey=jf011-sonarqube-demo-1 -Dsonar.host.url=http://localhost:9000 -Dsonar.login=9d466306414cebdcf29b112c0b0f9aee854e3126




