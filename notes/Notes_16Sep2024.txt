Javascript
-----------
ES6
ECMA script
ECMA = European Computer Manufacturers' Association



in a web application, there can be
	client side scripts
	server side scripts


javascript is run by a web browser

javascript is interpreted language


Difference between declaring variables using
	let		//ES6 
	var		//old


where do you mention the data type while declaring a variable in js?
	we do not mention.
	the data type is inferred from the value or reference assigned

(activity) There are dataypes in javasRIPT:

numbers
date
string
objects
tuples

constructs
	if
	if else
	if else if
	switch case
	for loop
	while loop
	do while 
	for each

Arrays


DOM
	Document Object Model


	document object represents the entire html document as a tree 
	we can access any element using name or id






for each loop in javascript:

	for(var x of arr)			x will get current element in each iteration


	for(var x in arr)			x will get the index in each iteration


<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        var arr=[];
        function fn1()
        {
            var country=document.getElementById("txtCountry").value;            
            arr.push(country);
            update();
        }

        function update()
        {
            var div=document.getElementById("div1");
            div.innerHTML="";
            for(var c of arr)
            {
                div.innerHTML+=c+"<br/>";
            }
        }

        function fnRemove()
        {
            arr.pop();
            update();
        }
    </script>
</head>
<body>
Enter the country: <input type="text" name="" id="txtCountry" >
<input type="button" value="Add" onclick="fn1()">    <br/>
<input type="button" value="Remove" onclick="fnRemove()"><br/>
<div id="div1"></div>
</body>
</html>



Objects
---------
	How to convert a JSON string into object



{id: '1', name: 'Gagan', department: 'CSE'}			this is object



<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        function fnAdd()
        {
            var emp={};
            var id=document.getElementById("id").value;
            var name=document.getElementById("name").value;
            var department=document.getElementById("department").value;
            
            emp.id=id;
            emp.name=name;
            emp.department=department;

            console.log(emp);

            console.log(JSON.stringify(emp))
            
        }

        function fnImport()
        {
            var txt=document.getElementById("txt1").value;
            //convert json string into object
            var emp=JSON.parse(txt);
            document.getElementById("id").value=emp.id;
            document.getElementById("name").value=emp.name;
            document.getElementById("department").value=emp.department;
        }
    </script>
</head>
<body>
    <input type="text" name="" id="txt1"><br>
    <input type="button" value="Import" onclick="fnImport()">

    Id: <input type="number" name="" id="id"><br>
    Name: <input type="text" name="" id="name"><br>
    Department: <input type="text" name="" id="department"><br>
    <input type="button" value="Add" onclick="fnAdd()">


</body>
</html>






Date:
----------
ISO format		yyyy-MM-dd

<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script>
        function fn1()
        {
            var dob=document.getElementById("dob").value;
            alert(dob)
        }
    </script>
</head>
<body>
    <input type="date" name="" id="dob">
    <input type="button" value="Submit" onclick="fn1()">
</body>
</html>


Activity:
---------
datatypes in javascript
programming constructs
	if
	switch
	while
	do while
	for loop
	for each loop
arrays

	push
	pop
	slice
	map	(important)
objects
	JSON
		stringify
		parse

try to use
	input type
		select
		radio
		checkbox

button click vs submit

<form>
	<input type="button" onclick="" />
</form>

vs

<form onSubmit="">
	<input type="submit" />
</form>


Template literal ``

we can use variables (values) in template literal
ex:
	var age=20;
	console.log("The value of age is ${age}");

<table>
<tr>
	<td>1</td>
	<td>Raja</td>
</tr>
</table>



Array methods
The push() method add the element to the end of the array.
The unshift() method add the element to the starting of the array.
The pop() method removes an element from the last index of the array.
The shift() method removes the element from the first index of the array.
The splice() method removes or replaces the element from the array.


length property
	array length can be changed using length property



// Creating an Array and Initializing with Values
let courses = ["HTML", "CSS", "Javascript", "React", "Node.js"];

// Increase the array length to 7
courses.length = 7;

console.log("Array After Increase the Length: ", courses);

// Decrease the array length to 2
courses.length = 2;
console.log("Array After Decrease the Length: ", courses)


Output
Array After Increase the Length:  [ 'HTML', 'CSS', 'Javascript', 'React', 'Node.js', <2 empty items> ]
Array After Decrease the Length:  [ 'HTML', 'CSS' ]


-----------------
// Creating an Array and Initializing with Values
let courses = ["HTML", "CSS", "JavaScript", "React"];

// Iterating through forEach loop
courses.forEach(function myfunc(elements) {
    console.log(elements);
});


Output
HTML
CSS
JavaScript
React




arrow functions			(similar to lambda in java 8)

-----------------------------------------------
https://www.geeksforgeeks.org/introduction-to-es6/

-----------------------------------------------------------

localStorage
sessionStorage

	localStorage.setItem(key,value);		//store an item 
	var value=localStorage.getItem(key);		//retrieve an item


	localStorage.removeItem(key);		//remove an item



Task:
-------
Create a home page where Login, Signup options are provided
Signup should allow users to signup
signedup users can login
on login, home page should welcome and greet the user.




raja,raja@123,Raja Siva Kumar;
siva,siva@123,Siva Raja;